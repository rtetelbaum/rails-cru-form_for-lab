<h1>New Artist Form</h1>

<%= form_for(@artist) do |f| %>
	<%= f.label :name, 'Name:' %><br>
	<%= f.text_field :name %><br>

	<%= f.label :bio, 'Bio:' %><br>
	<%= f.text_area :bio %><br>

	<%= f.submit %>
<% end %>